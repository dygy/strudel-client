---
// Main entry point - redirect to REPL
export const prerender = false;

// Always redirect to the main REPL page
// It will handle authentication internally
return Astro.redirect('/repl' + (Astro.url.search || ''));
---
